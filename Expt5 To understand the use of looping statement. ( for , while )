""" 
Program5A. Write a program to display vowels in the word taken as input from user and store 
them in a LIST. ( Use of “ for ” loop) 
""" 
vowels = ['a','e','i','o','u'] 
word = input("Enter any word: ") 
word = word.lower() 
found = [] 
for ch in word: 
 if ch in vowels: 
 if ch not in found: 
 found.append(ch) 
if len(found) == 0: 
 print(word , "Doesn't contain any vowel") 
for ele in found: 
 print(ele) # to display elements of list
print(found) # displaying list directly




""" 
Program5B. Write a program to count the number of times a vowel comes in a word taken from 
user and store them in DICTIONARY. ( Use of “ for ” loop) 
""" 
vowels = ['a','e','i','o','u'] 
word = input("Provide a word to search for vowels: ") 
lcword = word.lower() # lcword storing lowercase copy of word
found = {} 
for ch in lcword: 
 if ch in vowels: 
 if ch in found: 
 found[ch] += 1 
 else: 
 found[ch] = 1 
if len(found) != 0: 
 print("Vowel count is" , found) 
else: 
 print(word , "doesn't contain any vowel")
 
 
 """ 
Program5C. Using while loop write a program to add natural numbers upto n. Take value of n 
from user. i.e., sum = 1+2+3+...+n. ( Use of “ while ” loop) 
""" 
# To take input from the user 
n = int(input("Enter n: ")) 
# Initialize sum and counter 
sum = 0 
i = 1 
while i <= n: 
 sum = sum + i 
 i = i+1 # update/increment counter
# Print the sum 
print("The sum is" , sum) 



""" 
Program5D. Write a program for a clinic where patients are given a token number and they are 
called by their token number when doctor/physician press Y on his computer and stops when he 
presses N on the computer. ( Use of “ while ” loop) 
""" 
print("Hello everyone, Welcome to the clinic.") 
token = 1 
c = "Y" 
while(c == "Y"): 
 print("Token number" ,token, "may please come in") 
 c=input("Continue? (Y/N): ") # Asks whether to continue, Y will continue
 token=token+1 # Updating/Incrementing token number by 1
print("We are stopping here, Thank You!") 



